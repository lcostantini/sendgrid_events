<%= form_with(model: sendgrid_event, local: true) do |form| %>
  <% if sendgrid_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sendgrid_event.errors.count, "error") %> prohibited this sendgrid_event from being saved:</h2>

      <ul>
      <% sendgrid_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
